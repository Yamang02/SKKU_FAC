<%- /** * 페이지네이션 부분 템플릿 * 필요한 파라미터: * - pagination: 페이지네이션 정보 객체 * - baseUrl: 페이지 URL (예: "?page=" ) */ %>

    <div class="pagination">
        <% if (pagination.hasPrev) { %>
            <a href="<%= baseUrl %><%= pagination.currentPage - 1 %>" class="pagination__btn pagination__btn--prev"
                aria-label="이전 페이지">
                <i class="fas fa-chevron-left"></i>
            </a>
            <% } %>

                <% if (pagination.showFirstPage) { %>
                    <a href="<%= baseUrl %>1" class="pagination__btn">1</a>
                    <% } %>

                        <% if (pagination.showFirstEllipsis) { %>
                            <span class="pagination__ellipsis">...</span>
                            <% } %>

                                <% pagination.pages.forEach(pageNum=> { %>
                                    <% if (pageNum===pagination.currentPage) { %>
                                        <a href="<%= baseUrl %><%= pageNum %>"
                                            class="pagination__btn pagination__btn--active" aria-current="page">
                                            <%= pageNum %>
                                        </a>
                                        <% } else { %>
                                            <a href="<%= baseUrl %><%= pageNum %>" class="pagination__btn">
                                                <%= pageNum %>
                                            </a>
                                            <% } %>
                                                <% }); %>

                                                    <% if (pagination.showLastEllipsis) { %>
                                                        <span class="pagination__ellipsis">...</span>
                                                        <% } %>

                                                            <% if (pagination.showLastPage) { %>
                                                                <a href="<%= baseUrl %><%= pagination.totalPages %>"
                                                                    class="pagination__btn">
                                                                    <%= pagination.totalPages %>
                                                                </a>
                                                                <% } %>

                                                                    <% if (pagination.hasNext) { %>
                                                                        <a href="<%= baseUrl %><%= pagination.currentPage + 1 %>"
                                                                            class="pagination__btn pagination__btn--next"
                                                                            aria-label="다음 페이지">
                                                                            <i class="fas fa-chevron-right"></i>
                                                                        </a>
                                                                        <% } %>
    </div>
