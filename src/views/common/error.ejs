<div class="error-container">
    <h1>
        <% if (typeof error !=='undefined' && error.code===404) { %> 404 - 페이지를 찾을 수 없습니다 <% } else { %> 오류가 발생했습니다 <% }
                    %>
    </h1>
    <p class="error-message">
        <%= typeof message !=='undefined' ? message : '알 수 없는 오류가 발생했습니다.' %>
    </p>
    <% if (typeof error !=='undefined' && error.code) { %>
        <p class="error-code">에러 코드: <%= error.code %>
        </p>
        <% } %>
            <% if (typeof isDevelopment !=='undefined' && isDevelopment && typeof error !=='undefined' && error.stack) {
                %>
                <pre class="error-stack"><%= error.stack %></pre>
                <% } %>
                    <div class="button-container">
                        <a href="/" class="back-button"> 홈으로 돌아가기 </a>
                    </div>
</div>

<script type="module" src="/js/common/error.js"></script>
