<!DOCTYPE html>
<html lang="ko">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            <%= exhibition.title %> | <%= title %>
        </title>
        <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap"
            rel="stylesheet">
        <link rel="stylesheet" href="/css/style.css">
        <link rel="stylesheet" href="/css/pages/exhibition-detail.css">
    </head>

    <body>
        <%- include('../common/header') %>

            <main>
                <section class="exhibition-detail-container">
                    <div class="exhibition-hero">
                        <div class="exhibition-hero-image" style="background-image: url('<%= exhibition.image %>')">
                            <div class="exhibition-hero-overlay"></div>
                            <div class="exhibition-hero-content">
                                <h1 class="exhibition-title">
                                    <%= exhibition.title %>
                                </h1>
                                <% if (exhibition.subtitle) { %>
                                    <p class="exhibition-subtitle">
                                        <%= exhibition.subtitle %>
                                    </p>
                                    <% } %>
                            </div>
                        </div>
                    </div>

                    <div class="exhibition-content">
                        <div class="exhibition-info">
                            <div class="exhibition-meta">
                                <div class="exhibition-meta-item">
                                    <h3 class="exhibition-meta-title">전시 기간</h3>
                                    <p class="exhibition-meta-value">
                                        <%= exhibition.startDate %> ~ <%= exhibition.endDate %>
                                    </p>
                                </div>
                                <div class="exhibition-meta-item">
                                    <h3 class="exhibition-meta-title">전시 장소</h3>
                                    <p class="exhibition-meta-value">성균관대학교 인문사회캠퍼스 600주년기념관 1층</p>
                                </div>
                                <div class="exhibition-meta-item">
                                    <h3 class="exhibition-meta-title">참여 작가</h3>
                                    <p class="exhibition-meta-value">
                                        <%= exhibition.artists.join(', ') %></p>
                                </div>
                            </div>

                            <div class="exhibition-description">
                                <h2 class="section-title">전시 소개</h2>
                                <div class="exhibition-description-content">
                                    <%- exhibition.description %>
                                </div>
                            </div>
                        </div>

                        <div class="exhibition-artworks">
                            <h2 class="section-title">전시 작품</h2>
                            <div class="artwork-grid">
                                <% artworks.forEach(artwork => { %>
                                    <div class="card card--exhibition">
                                        <a href="/artwork/<%= artwork.id %>" class="card__link">
                                            <img src="<%= artwork.image %>" alt="<%= artwork.title %>" class="card__image">
                                            <div class="card__info">
                                                <h3 class="card__title"><%= artwork.title %></h3>
                                                <p class="card__subtitle"><%= artwork.artist %></p>
                                            </div>
                                        </a>
                                    </div>
                                <% }); %>
                            </div>
                        </div>
                    </div>
                </section>
            </main>

            <%- include(' ../common/footer') %>

                                            <script src="/js/exhibition/detail.js"></script>
    </body>

</html>
