<!DOCTYPE html>
<html lang="ko">

    <head>
        <% const additionalStyles=['/css/common/error.css']; %>
            <%- include('./head', { pageTitle: '오류' , additionalStyles }) %>
    </head>

    <body>
        <%- include('./header') %>

            <main class="error-page">
                <div class="error-container">
                    <h1>오류가 발생했습니다</h1>
                    <p>
                        <%= typeof error !=='undefined' && error.message ? error.message : '알 수 없는 오류가 발생했습니다.' %>
                    </p>
                    <a href="/" class="back-button">메인 페이지로 돌아가기</a>
                </div>
            </main>

            <%- include('./footer') %>
    </body>

</html>
