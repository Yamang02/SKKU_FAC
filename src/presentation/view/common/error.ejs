<!DOCTYPE html>
<html lang="ko">

    <head>
        <% const additionalStyles=['/css/common/error.css']; %>
            <%- include('./head', { pageTitle: '오류' , additionalStyles }) %>
    </head>

    <body>
        <%- include('./header') %>

            <main class="error-page">
                <div class="error-container">
                    <h1>
                        <%= title || '오류가 발생했습니다' %>
                    </h1>
                    <p class="error-message">
                        <%= message %>
                    </p>
                    <% if (typeof error !=='undefined' && error.code) { %>
                        <p class="error-code">에러 코드: <%= error.code %>
                        </p>
                        <% } %>
                            <% if (typeof error !=='undefined' && error.stack && process.env.NODE_ENV==='development' )
                                { %>
                                <pre class="error-stack"><%= error.stack %></pre>
                                <% } %>
                                    <div class="button-container">
                                        <% const finalReturnUrl=typeof returnUrl !=='undefined' ? returnUrl : '/' ; %>
                                            <a href="<%= finalReturnUrl %>" class="back-button">
                                                <%= finalReturnUrl==='/admin' ? '대시보드로' : '이전 페이지로' %> 돌아가기
                                            </a>
                                    </div>
                </div>
            </main>

            <%- include('./footer') %>
    </body>

</html>
