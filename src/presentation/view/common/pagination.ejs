<%- /** * 페이지네이션 부분 템플릿 * 필요한 파라미터: * - pagination: 페이지네이션 정보 객체 * - baseUrl: 페이지 URL (예: "?commentPage=" ) */ %>

    <div class="pagination">
        <% if (pagination.hasPrev) { %>
            <a href="<%= baseUrl %><%= pagination.currentPage - 1 %>" class="pagination__btn pagination__btn--prev">
                <i class="fas fa-chevron-left"></i>
            </a>
            <% } %>

                <% for(let i=1; i <=pagination.totalPages; i++) { %>
                    <% if (i===pagination.currentPage) { %>
                        <a href="<%= baseUrl %><%= i %>" class="pagination__btn pagination__btn--active">
                            <%= i %>
                        </a>
                        <% } else { %>
                            <a href="<%= baseUrl %><%= i %>" class="pagination__btn">
                                <%= i %>
                            </a>
                            <% } %>
                                <% } %>

                                    <% if (pagination.hasNext) { %>
                                        <a href="<%= baseUrl %><%= pagination.currentPage + 1 %>"
                                            class="pagination__btn pagination__btn--next">
                                            <i class="fas fa-chevron-right"></i>
                                        </a>
                                        <% } %>
    </div>
