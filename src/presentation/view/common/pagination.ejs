<% /** * 페이지네이션 부분 템플릿 * * 필요한 파라미터: * - pagination: 페이지네이션 정보 객체 * - baseUrl: 페이지 URL (예: "?commentPage=" ) */ %>

    <div class="pagination">
        <% if (pagination.hasPrevPage) { %>
            <a href="<%= baseUrl %><%= pagination.currentPage - 1 %>" class="page-btn prev" aria-label="이전 페이지">
                <i class="fas fa-chevron-left"></i>
            </a>
            <% } else { %>
                <span class="page-btn prev disabled" aria-label="이전 페이지">
                    <i class="fas fa-chevron-left"></i>
                </span>
                <% } %>

                    <% if (pagination.showFirstPage) { %>
                        <a href="<%= baseUrl %>1" class="page-btn">1</a>
                        <% if (pagination.showFirstEllipsis) { %>
                            <span class="page-dots">...</span>
                            <% } %>
                                <% } %>

                                    <% pagination.pages.forEach(pageNum=> { %>
                                        <a href="<%= baseUrl %><%= pageNum %>"
                                            class="page-btn <%= pageNum === pagination.currentPage ? 'active' : '' %>">
                                            <%= pageNum %>
                                        </a>
                                        <% }); %>

                                            <% if (pagination.showLastPage) { %>
                                                <% if (pagination.showLastEllipsis) { %>
                                                    <span class="page-dots">...</span>
                                                    <% } %>
                                                        <a href="<%= baseUrl %><%= pagination.totalPages %>"
                                                            class="page-btn">
                                                            <%= pagination.totalPages %>
                                                        </a>
                                                        <% } %>

                                                            <% if (pagination.hasNextPage) { %>
                                                                <a href="<%= baseUrl %><%= pagination.currentPage + 1 %>"
                                                                    class="page-btn next" aria-label="다음 페이지">
                                                                    <i class="fas fa-chevron-right"></i>
                                                                </a>
                                                                <% } else { %>
                                                                    <span class="page-btn next disabled"
                                                                        aria-label="다음 페이지">
                                                                        <i class="fas fa-chevron-right"></i>
                                                                    </span>
                                                                    <% } %>
    </div>
