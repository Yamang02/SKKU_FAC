<%- include('../../../common/head', { pageTitle: '사용자 관리' }) %>

    <div class="admin-container">
        <%- include('../../component/sidebar') %>

            <main class="admin-main">
                <div class="admin-content">
                    <div class="content-header">
                        <h1>사용자 관리</h1>
                    </div>

                    <div class="content-body">
                        <div class="card">
                            <div class="card-header">
                                <h2>사용자 목록</h2>
                                <button class="btn btn-primary">새 사용자 추가</button>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>이름</th>
                                            <th>이메일</th>
                                            <th>역할</th>
                                            <th>상태</th>
                                            <th>작업</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <!-- 사용자 목록이 여기에 동적으로 추가됩니다 -->
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
    </div>

    <%- include('../../../common/footer') %>
