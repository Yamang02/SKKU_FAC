<!DOCTYPE html>
<html lang="ko">

    <head>
        <% const pageTitle='회원가입 - SKKU FAC Gallery' ; const additionalStyles=['/css/user/Register.css']; %>
            <%- include('../common/head', { pageTitle, additionalStyles }) %>
    </head>

    <body>
        <%- include('../common/header') %>

            <main class="register-page">
                <div class="container">
                    <div class="register-card">
                        <h2 class="register-title">회원가입</h2>
                        <% if (locals.error) { %>
                            <div class="alert alert-danger">
                                <%= error %>
                            </div>
                            <% } %>

                                <form class="register-form" action="/user/register" method="POST">
                                    <div class="form-group">
                                        <input type="text" id="username" name="username" class="form-control"
                                            value="<%= locals.formData?.username || '' %>" required
                                            pattern="^[a-zA-Z0-9_]{4,20}$" placeholder="아이디">
                                    </div>

                                    <div class="form-group">
                                        <div class="password-input-group">
                                            <input type="password" id="password" name="password" class="form-control"
                                                required
                                                pattern="^(?=.*[A-Za-z])(?=.*\d)(?=.*[@$!%*#?&])[A-Za-z\d@$!%*#?&]{8,}$"
                                                placeholder="비밀번호">
                                            <button type="button" class="toggle-password" aria-label="비밀번호 표시">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                        </div>
                                    </div>

                                    <div class="form-group">
                                        <div class="password-input-group">
                                            <input type="password" id="confirmPassword" name="confirmPassword"
                                                class="form-control" required placeholder="비밀번호 확인">
                                            <button type="button" class="toggle-password" aria-label="비밀번호 표시">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                        </div>
                                    </div>

                                    <div class="form-group">
                                        <input type="text" id="name" name="name" class="form-control"
                                            value="<%= locals.formData?.name || '' %>" required placeholder="이름">
                                    </div>

                                    <div class="form-group">
                                        <input type="email" id="email" name="email" class="form-control"
                                            value="<%= locals.formData?.email || '' %>" required placeholder="이메일">
                                    </div>

                                    <div class="form-group">
                                        <select id="role" name="role" class="form-control" required>
                                            <option value="">가입 유형을 선택하세요</option>
                                            <option value="MEMBER">동아리 회원</option>
                                            <option value="ARTIST">작가</option>
                                            <option value="VISITOR">일반 방문자</option>
                                        </select>
                                    </div>

                                    <div class="club-member-field">
                                        <div class="form-group">
                                            <input type="text" id="studentId" name="studentId" class="form-control"
                                                value="<%= locals.formData?.studentId || '' %>" pattern="[0-9]{10}"
                                                placeholder="학번 (10자리)">
                                        </div>
                                    </div>

                                    <div class="artist-field">
                                        <div class="form-group">
                                            <textarea id="artistBio" name="artistInfo[biography]" class="form-control"
                                                placeholder="작가 소개"><%= locals.formData?.artistInfo?.biography || '' %></textarea>
                                        </div>
                                        <div class="form-group">
                                            <input type="url" id="artistWebsite" name="artistInfo[website]"
                                                class="form-control"
                                                value="<%= locals.formData?.artistInfo?.website || '' %>"
                                                placeholder="웹사이트 (선택사항)">
                                        </div>
                                    </div>

                                    <div class="form-group">
                                        <button type="submit" class="btn btn-primary">회원가입</button>
                                    </div>

                                    <div class="login-link">
                                        이미 계정이 있으신가요? <a href="/user/login">로그인</a>
                                    </div>
                                </form>
                    </div>
                </div>
            </main>

            <%- include('../common/footer') %>
                <script src="/js/page/user/Register.js"></script>
    </body>

</html>
